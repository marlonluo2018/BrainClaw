---
description: OpenCode entrypoint for the BrainClaw email-classifier subagent.
mode: subagent
---

# Email Sync Sub-Agent (OpenCode Entry Point)

Before performing any classification, task update, ignore registration, or summary work, read `assistant_brain/agents/opencode/email-classifier.md` completely and follow it as the authoritative OpenCode SOP.

This file exists only because OpenCode discovers this registered subagent from `.opencode/agents/`. Maintain the OpenCode SOP in `assistant_brain/agents/opencode/email-classifier.md`, not here.